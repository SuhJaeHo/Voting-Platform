<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="vote_outline">
      <% if (votes) { %>
        <% if (votes.length === 0) { %>
          <div>
            <h1>등록된 투표가 없습니다</h1>
          </div>
        <% } %>
        <% for (const vote of votes) { %>
          <div class="voteInfo" onclick="location.href='/votings/<%= vote._id %>'">
            <p>생성자 : <%= vote.creatorName %></p>
            <p>제목 : <%= vote.title %></p>
            <p>투표 만료시간 : <%= vote.expiredTime %></p>
            <p><%= vote.isExpired %></p>
          </div>  
        <% } %>
      <% } %>
    </div>
    <div>
      <button
        onclick="location.href='/votings/new'";
      >
        투표생성
      </button>
    </div>
    <% if (user) { %>
      <a href="/logout">로그아웃</a>
      <a href="/my-votings">마이페이지</a>
    <% } %>
  </body>
  <script type="text/javascript">
    if ("<%= success %>" === "true") {
      location.href="/votings/success"
    }
  </script>
</html>
